# 已安排动作卡片化测试报告

日期：2026-09-16

## 覆盖范围

- 已安排动作列表从「与上方添加动作区同构的整宽行」改为四列网格的横置长方形卡片。
- 卡片内的层次：动作名 → 组数 / 次数 / 重量 → 编辑与移除 → 折叠的完成记录。
- 响应式列数：桌面 4 列、≤1000px 3 列、≤700px 2 列。
- 卡片内编辑浮层改为向上弹出，避免窄卡片下遮挡下方内容或溢出视口。

## 验证结果

- 组件层 `npx vitest run`：14 个测试文件、105 项测试全部通过；
  `tests/plan-editor.test.tsx` 的 `data-testid="planned-row"` 契约未变。
- 浏览器实测（Chromium，真实 dev server）：
  - 桌面宽度解析为 **4 列**，单卡 **213 × 159**，宽大于高（横置）成立。
  - 断点实测：1000px → 3 列、700px → 2 列、420px → 2 列。
  - 卡片内编辑浮层落在视口内（x=216、宽 268），未被裁切。
  - `console_errors=0`。
- CSS 语法自检：空选择器 / 双逗号 / 逗号接右括号 / 行首裸组合符均为 0，花括号 850/850 配平。

## 改动范围

- `app/globals.css`：11 行替换为 11 行（`.ex-list` 网格化、`.exercise` 卡片化、卡片内字号与间距、
  `.pop` 改向上弹出、`.recap` 加分隔线、两处响应式列数）。无其他文件改动。
