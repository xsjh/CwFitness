# 用户头像菜单单元测试 — 2026-09-15

**命令：** `$env:CWFITNESS_DEV_DB_NAME='codex-test-no-instance'; node node_modules/vitest/vitest.mjs run --coverage`
**结果：** 通过 93，失败 0

| 目标 | 用例数 | 通过 | 失败 |
| --- | --- | --- | --- |
| `app/user-menu.tsx` | 4 | 4 | 0 |

## 已覆盖

- User 菜单触发器显示用户名、`@账号` 与文字头像，同时不再暴露完整邮箱。
- 点击头像打开二级菜单，占位功能明确标记为尚不可用，退出登录仍可使用。
- 按 `Escape` 关闭菜单后，焦点返回头像触发器。
- User 点击菜单外部时关闭菜单；工作区忙碌时禁用退出登录。

## 未覆盖

- 菜单在真实背景图上的玻璃混合、锚点位置和响应式尺寸 — 已通过本地浏览器人工视觉检查，单元测试不适合判断视觉质感。
- `onSignOut` 的网络请求和登录页跳转 — 由现有认证集成测试与浏览器测试负责。

## 覆盖率

| 文件 | 语句 | 行 | 分支 | 函数 |
| --- | --- | --- | --- | --- |
| `app/user-menu.tsx` | 96.15% | 100% | 69.23% | 100% |

全量 Vitest 覆盖率：语句 28.66%，行 32.45%，分支 21.09%，函数 27.17%。`app/api/` 主要由实时服务上的 HTTP 集成测试覆盖，因此这里的全局数字只是单元层下限。
