# CwFitness 训练工作台设计规范

**状态：** 已实现  
**适用范围：** 用户登录后的训练工作台，包括今日、计划、动作、历史、进展，以及进行中的 Workout Session。  
**实现入口：** `app/workout-workspace.tsx`、`app/training-panel.tsx`、`app/user-menu.tsx`、`app/globals.css`

## 1. 设计意图

训练工作台是一个深色、低干扰的日常工具：用户进入后，应立刻理解今天该练什么、能否开始或继续一场 Workout Session，以及如何抵达计划、动作、历史和进展。

视觉重点按以下顺序排列：

1. 当前训练或推荐的 Workout Day。
2. 记录一组、挂起、结束等不可替代的训练操作。
3. 训练计划与历史数据。
4. 账户与设置。

界面以稳定的左侧栏建立空间定位；不要重新引入顶部 tab 条或将账户入口移回右上角。

## 2. 设计令牌

| 用途 | 值 | 说明 |
| --- | --- | --- |
| 页面底色 | `#090b0c` | 近石墨黑，降低长时间训练记录的视觉疲劳。 |
| 主要文字 | `rgba(255,255,255,.96)` | 标题、关键数字和可操作文字。 |
| 次级文字 | `rgba(255,255,255,.62)` | 说明、元数据、禁用状态。 |
| 分隔线 | `rgba(255,255,255,.12)` | 卡片、行和控件边界。 |
| 训练强调色 | `#c2db86` | 当前导航、主操作、完成状态；不可用于危险操作。 |
| 危险色 | `#e9a095` | 放弃、删除等不可逆操作。 |
| 动效曲线 | `cubic-bezier(.23,1,.32,1)` | 仅用于短暂的进入/反馈动效。 |

页面使用系统优先的无衬线字体栈；标题以负字距和紧凑行高建立层级。圆角以用途区分：侧栏导航 `10px`、统计卡 `18px`、今日主卡 `24px`。

## 3. 信息架构

### 3.1 左侧栏

桌面端左栏固定为 `248px` 宽、高度为视口高度，分为两个区域：

- 顶部：CwFitness 标识、`训练空间` 标签、主导航。
- 底部：`账户` 标签与用户菜单。用户菜单向上弹出，避免超出视口。

主导航顺序固定为：`今日`、`计划`、`动作`、`历史`、`进展`。每项使用无文本内容的装饰图标（通过 CSS `data-icon` 生成）；图标必须 `aria-hidden`，按钮的可访问名称只保留中文导航名称。

当前入口使用浅绿色实底和深色文字。进行中的 Workout Session 仍将“今日”标为当前入口，左栏必须保持可访问。

### 3.2 今日页

今日页采用两列布局：主训练卡占较大比例，统计卡位于右侧。

- 主训练卡展示三种业务状态：已有 In-progress Session、可开始的推荐 Workout Day、尚未建立计划时的引导。
- 卡片使用深绿至石墨黑渐变、低对比同心圆装饰和一枚高优先级行动按钮。
- 统计区域可显示训练计划、Exercise、Workout Day、今日 Training Time、最近完成日期与 Progression Suggestion。统计数据缺失时应使用 `—`，不伪造数据。

标题和训练状态位于内容顶部，说明文字不得与主操作竞争视觉权重。

### 3.3 Workout Session 页面

Workout Session 是专注的记录页面，但不脱离工作台。页面内容顺序为：

1. 状态标题与会话操作（接管、挂起/继续、结束、放弃）。
2. 摘要：完成组数、训练日期、动作数、状态。
3. 追加 Exercise 表单。
4. Exercise 列表；每个 Exercise 展示目标、排序、移除/跳过操作及各组记录行。

每一组的“记录完成”是主要频繁操作；更新记录、跳过和管理动作是次级操作。只读、离线或挂起状态必须保留现有禁用逻辑，视觉样式不得暗示可执行。

### 3.4 其他工作台视图

计划、动作、历史和进展沿用同一左栏与深色背景，不改变现有业务组件、数据请求或领域用语。它们应使用现有的边框、深色半透明表面和绿色强调色，而非复制“今日”主卡的强装饰。

## 4. 布局与断点

| 视口 | 导航形态 | 主内容 |
| --- | --- | --- |
| 大于 `1000px` | `248px` 完整左栏，显示文字与账户信息。 | 今日页为主卡 + 统计卡两列。 |
| `701–1000px` | `76px` 图标左栏；账户触发器仅显示头像。 | 今日页变为单列，统计卡为三列网格。 |
| 小于等于 `700px` | 底部固定图标导航；账户菜单不在底栏显示。 | 内容左右留白 `18px`；今日页单列，统计卡两列。 |

移动端底栏是唯一例外：它防止 76px 左栏挤压训练记录表单。为避免被底栏遮挡，工作台内容底部保留至少 `74px` 空间。

## 5. 交互与状态

- 导航切换即时发生；不要为高频 tab 切换增加过渡动画。
- 按钮按下可使用短暂缩放或颜色反馈；动效不超过 `300ms`，优先使用 `opacity` 与 `transform`。
- `workspace-notice` 是跨视图状态反馈，固定在页面上方，不应被侧栏覆盖。
- 账户菜单由头像触发；支持点击外部关闭与 `Escape` 关闭，关闭后焦点回到触发按钮。
- 训练结束进入历史，放弃返回今日；这些既有状态流转不得由视觉重构改变。

## 6. 可访问性与偏好设置

- 所有导航和操作必须是原生 `button`，保留可见焦点轮廓。
- 主导航使用 `aria-current="page"` 标识当前视图；`主要导航` 是其可访问标签。
- 浅绿色激活项上的文本必须保持深色，以满足可读性。
- `prefers-reduced-motion: reduce` 时，移除位移动画，仅保留短淡入或无动画。
- `prefers-reduced-transparency: reduce` 时，侧栏与卡片改为不透明深色背景并关闭模糊。
- `prefers-contrast: more` 时，增强侧栏、主卡、统计卡边框，并为当前导航提供白色轮廓。

## 7. 实现边界

- 布局状态保留在 `WorkoutWorkspace`；不要将导航状态复制到子组件。
- `TrainingPanel` 继续只负责 Workout Session 的记录界面和事件回调。
- `UserMenu` 继续负责账户、设置与退出行为；侧栏仅决定其摆放位置。
- 只可在 `app/globals.css` 的“Focus training console”规则之后追加该工作台的覆盖规则，避免影响欢迎页和认证页面。
- 任何新增导航入口必须同步更新 `WorkspaceView`、导航按钮、键盘/浏览器测试辅助函数和本文件。

## 8. 验收清单

- [ ] 桌面端显示固定左栏，账户入口在左下角。
- [ ] 今日页显示训练主卡和统计卡，且可开始或继续 Workout Session。
- [ ] 进行中的 Workout Session 仍可访问左栏与账户菜单。
- [ ] 窄屏左栏正确退化为底部图标导航，内容不被遮挡。
- [ ] 所有导航项仍可由键盘访问并正确更新 `aria-current`。
- [ ] 离线、只读、挂起、同步失败与冲突状态仍使用现有业务逻辑。
